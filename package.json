{
  "engines": {
    "node": "14.18.1"
  },
  "scripts": {
    "build": "npm run install-all && npx webpack && cd ./frontend && npm run build",
    "start": "node server.js",
    "dev": "nodemon server.js",
    "install-all": "npm install && cd frontend && npm install"
  },
  "dependencies": {
    "dotenv": "^10.0.0",
    "expose-loader": "^3.1.0",
    "express": "^4.17.1",
    "morgan": "^1.10.0",
    "nodemon": "^2.0.15",
    "ts-loader": "^9.2.6",
    "typescript": "^4.5.2",
    "webpack": "^5.64.1",
    "webpack-cli": "^4.9.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.13"
  }
}
