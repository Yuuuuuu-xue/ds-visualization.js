namespace ds {
  export const canvasStyle = (width: number, height: number) => `
    width: ${width}px;
    height: ${height}px;
    border: 2px solid crimson;
    display: relative;
    padding: 5px;
    overflow: auto;
  `;
}